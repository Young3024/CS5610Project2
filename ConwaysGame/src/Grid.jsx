import React from 'react';

const Grid = ({ size }) => {
  const { height, width } = size;

  // Logic for rendering grid goes here

  return (
    <div className="grid">
      {/* Render grid cells */}
    </div>
  );
}

export default Grid;
